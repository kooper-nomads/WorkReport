# Used only when running `npm run dev` outside Docker. Under `docker compose up`
# this is ignored — Vite prefers the already-set process env, which docker-compose.yml
# derives from the root .env's BACKEND_PUBLIC_URL instead.
VITE_API_URL=http://localhost:3001
